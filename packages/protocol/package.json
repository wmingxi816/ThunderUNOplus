{
  "name": "@thunder-uno/protocol",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": ["dist"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "corepack pnpm --filter @thunder-uno/shared-types build && tsc -p tsconfig.json --noEmit",
    "test": "corepack pnpm --filter @thunder-uno/shared-types build && vitest run"
  },
  "dependencies": {
    "@thunder-uno/shared-types": "workspace:*"
  }
}
