# 雷霆 UNOplus UI 设计文档

## 1. 当前设计目标

当前网页端已经具备大厅、房间、对战、出牌、摸牌、UNO、质疑和结束结算等完整试玩能力。下一轮 UI 调整的重点不是新增规则，而是把对战界面从“网页面板布局”改成“整屏沉浸式牌桌”。

核心目标：

1. 对战界面背景覆盖整个浏览器视口。
2. 进入对局后不再显示可上下滑动的网页菜单式结构。
3. 所有手牌、按钮、状态提示都放在同一个对战区里。
4. 保留大厅页的调试和联调能力。
5. 不改变服务端权威、协议收口和 `uno-core` 规则归属。

## 2. 现状问题

当前对战页虽然已经有 `battle-stage` 背景和牌桌元素，但整体结构仍然像普通网页：

1. 顶部仍有标题栏。
2. 标题栏下方仍有连接面板。
3. 对战区、手牌区、按钮区和日志区是纵向排列的独立模块。
4. 页面高度超过视口时会产生上下滚动。
5. 手机端试玩时，用户容易把页面当成网页滚动，而不是一个固定的游戏画面。
6. 手牌和操作按钮被隔离在战场之外，削弱了“正在牌桌上操作”的感觉。

这类问题不会影响规则正确性，但会影响真实试玩时的沉浸感、触控稳定性和视觉统一度。

## 3. 总体设计方向

对战状态下采用“整屏牌桌”结构：

```txt
┌────────────────────────────────────────────┐
│  背景图覆盖整个视口                         │
│                                            │
│  顶部/四周：对手座位、连接状态、退出按钮     │
│                                            │
│              中央牌桌区域                  │
│        摸牌堆 / 弃牌堆 / 方向 / 加牌链       │
│                                            │
│  底部：自己座位、手牌、出牌/摸牌/UNO 按钮    │
└────────────────────────────────────────────┘
```

大厅状态继续使用原来的网页式结构，方便输入服务端地址、昵称、房间号和查看日志。只有进入 `snapshot !== null` 的 playing / finished 对战状态后，才切换到沉浸式布局。

## 4. 页面根结构设计

### 4.1 大厅状态

大厅仍保留：

1. 顶部标题 `雷霆UNOplus`。
2. WebSocket 服务端输入。
3. 昵称输入。
4. 连接、断开、Ping。
5. 创建房间、加入房间、离开房间。
6. 等待房间玩家列表。
7. 日志面板。

大厅的目标是“组局控制台”，允许开发、联调和局域网试玩快速定位问题。

### 4.2 对战状态

对战状态需要切换为：

1. 根容器占满 `100dvh`。
2. 页面 `body` 或对战根容器禁止纵向滚动。
3. 隐藏或移出普通网页顶部标题栏。
4. 隐藏连接表单和日志面板。
5. 连接状态、错误提示和退出入口改为对战 HUD。
6. 战斗背景覆盖整个页面，而不是只覆盖一个面板。

建议给根容器增加状态类，例如：

```txt
shell shell-battle
```

或者：

```txt
main.battle-shell
```

当 `state.snapshot !== null` 时使用沉浸式结构；当返回大厅后恢复普通结构。

## 5. 背景与战场设计

### 5.1 背景覆盖

对战背景使用现有 `apps/client-web/public/battle-stage.png`，但展示范围从 `battle-stage` 面板扩展为整个视口。

设计要求：

1. `background-size: cover`。
2. `background-position: center`。
3. 使用深色遮罩保证牌面、按钮和文字可读。
4. 不使用单独卡片容器包住整个战场。
5. 对战背景不能随着手牌数量变化而撑高页面。

### 5.2 战场层级

对战界面按层级组织：

1. 背景层：全屏背景图和遮罩。
2. 座位层：对手和自己座位。
3. 桌面层：摸牌堆、弃牌堆、当前颜色、方向、加牌链。
4. 操作层：手牌、出牌按钮、摸牌按钮、UNO 按钮。
5. 弹层层：颜色选择、质疑、摸牌后立即打出 / 保留、胜负结算。
6. 提示层：错误提示、事件 toast、连接状态。

## 6. 对手座位设计

### 6.1 桌面端

桌面端对手座位围绕牌桌分布：

1. 左侧座位：左侧玩家。
2. 顶部座位：上方玩家。
3. 右侧座位：右侧玩家。
4. 多人时继续沿上半区和两侧分布。

每个对手座位显示：

1. 头像。
2. 昵称。
3. 手牌数量。
4. 当前状态，例如当前回合、UNO、已退出、断线。
5. 必要时显示最近动作提示，例如摸牌、被罚抽、出牌。

### 6.2 移动端

移动端不强行复刻桌面环绕布局，优先保证可读性：

1. 竖屏时，对手座位压缩到顶部横向区域。
2. 横屏时，可以恢复更接近桌面端的左右分布。
3. 对手卡片尺寸要稳定，不能因为昵称长短挤压中央牌桌。
4. 手牌数量优先显示，昵称过长时截断。

## 7. 中央牌桌设计

中央牌桌是对战画面的视觉焦点。

必须包含：

1. 摸牌堆。
2. 弃牌堆。
3. 当前有效颜色。
4. 当前方向。
5. 加牌链数量。
6. 当前玩家提示。
7. 出牌和摸牌动画落点。

设计要求：

1. 中央牌桌在视口中部偏上，给底部手牌留空间。
2. 弃牌堆保留散落堆叠效果。
3. 加牌链用醒目的数字或标签贴近中央牌堆。
4. 方向图标和当前颜色不要挤在日志式文字里，要成为桌面状态的一部分。
5. 当前玩家提示要短，避免覆盖牌面。

## 8. 自己座位与手牌设计

### 8.1 手牌位置

自己的手牌固定在战场底部，属于战场的一部分，而不是独立面板。

设计要求：

1. 手牌区贴近底部安全区。
2. 手牌最多占用视口下方一块固定区域。
3. 手牌过多时，只允许手牌内部滚动或换行，不允许整个网页滚动。
4. 选中的牌继续上移动效。
5. 可出牌、不可出牌、组合候选、冲突牌继续保留视觉状态。

### 8.2 手牌多时的处理

桌面端：

1. 手牌可以横向展开。
2. 极多手牌时允许两行或局部横向滚动。
3. 不让手牌把中央牌桌顶走。

移动端：

1. 竖屏优先使用横向滚动。
2. 横屏可以使用更扁的卡牌尺寸。
3. 按钮和手牌之间要留出触控间距。
4. 手牌局部滚动时不要触发页面整体滚动。

## 9. 操作按钮设计

所有对战操作按钮都放在对战区内。

按钮分组：

1. 主操作：出牌、摸牌。
2. UNO 操作：喊 UNO、抓 UNO。
3. 辅助操作：保留摸到的牌、取消选择、退出房间。
4. 状态操作：返回大厅、重新连接。

布局建议：

1. 主操作按钮靠近手牌右侧或手牌上方。
2. UNO 按钮靠近主操作按钮，但颜色和层级与出牌区分。
3. 退出房间放在右上角 HUD，不放在手牌附近，避免误触。
4. 错误提示以 toast 或底部短提示显示，不再占用独立错误行。

按钮要求：

1. 触控尺寸不小于当前按钮尺寸。
2. 禁用态仍然清晰可见。
3. 不能因文字过长撑破布局。
4. 关键按钮在移动端不能被浏览器底部安全区遮挡。

## 10. 连接状态与日志处理

对战中不再显示完整连接表单和日志面板。

保留方式：

1. 连接状态改为左上角或右上角小徽标。
2. 错误信息改为短 toast。
3. 重要事件继续通过动画、弹窗或 toast 表达。
4. 详细日志只在大厅显示，或者后续做成调试开关。

这样可以避免对战时出现“上面是游戏，下面是网页调试面板”的割裂感。

## 11. 弹层设计

以下弹层仍然覆盖在对战区最上方：

1. 黑牌颜色选择。
2. 质疑窗口。
3. 摸牌后立即打出 / 保留。
4. 胜负结算。
5. 断线或房间关闭提示。

弹层要求：

1. 居中显示。
2. 背后战场轻微压暗。
3. 弹层不使用过大的说明文字。
4. 弹层按钮清楚区分主次。
5. 弹层打开时避免误触背后的牌和按钮。

## 12. 移动端设计

### 12.1 竖屏

竖屏目标是“可玩优先”：

1. 顶部压缩对手座位。
2. 中央牌桌缩小但保持摸牌堆和弃牌堆可点击。
3. 底部固定手牌和操作按钮。
4. 页面整体不滚动。
5. 手牌区可以局部横向滚动。

### 12.2 横屏

横屏目标是“接近桌面牌桌”：

1. 左右两侧可以放对手座位。
2. 中央牌桌扩大。
3. 手牌贴底横向铺开。
4. 操作按钮放在手牌附近的右侧或上方。

### 12.3 安全区

移动端必须处理：

1. `safe-area-inset-bottom`。
2. 地址栏导致的高度变化，优先使用 `100dvh`。
3. 极窄屏下按钮换行。
4. 长昵称、极多手牌、弹层同时出现时不重叠。

## 13. 实施步骤

### 第一步：切换对战根布局

目标：

1. 当 `state.snapshot === null` 时继续渲染大厅结构。
2. 当 `state.snapshot !== null` 时渲染沉浸式对战结构。
3. 对战结构不再输出普通 topbar、connection panel、log panel。

涉及文件：

1. `apps/client-web/src/main.ts`
2. `apps/client-web/src/styles.css`

验收：

1. 进入大厅时仍能连接、建房、加入房间。
2. 进入对战后页面不出现顶部连接表单。
3. 进入对战后日志面板不占据页面高度。

### 第二步：让背景覆盖整个网页

目标：

1. 对战根容器使用 `min-height: 100dvh` 或 `height: 100dvh`。
2. 背景图覆盖整个视口。
3. `body` 在对战状态不出现纵向滚动。

验收：

1. 桌面端 1280x720 下无页面纵向滚动。
2. 手机竖屏下无整页上下滑动。
3. 背景不因手牌增多而被撑开。

### 第三步：把手牌区并入战场

目标：

1. 移除对战页里独立面板感的手牌容器。
2. 手牌固定在战场底部。
3. 操作按钮与手牌一起成为战场 HUD。

验收：

1. 选牌、取消选择、出牌仍正常。
2. 组合候选高亮仍正常。
3. 极多手牌时中央牌桌不被挤出视口。

### 第四步：把按钮放进对战 HUD

目标：

1. 出牌、摸牌、UNO、抓 UNO 放在战场内。
2. 退出房间放在右上角 HUD。
3. 错误提示改为战场内 toast。

验收：

1. 所有原有按钮功能不变。
2. 禁用原因仍通过 title 或提示表达。
3. 对战结束后“返回大厅”仍可用。

### 第五步：移动端收口

目标：

1. 竖屏无整页滚动。
2. 横屏保留牌桌感。
3. 手牌可局部滚动或稳定换行。
4. 弹层不遮挡关键按钮到不可操作。

验收：

1. 390x844 竖屏可完成一次出牌或摸牌。
2. 横屏可看清对手、中央牌堆和自己手牌。
3. 长昵称不会撑破座位卡片。

### 第六步：回归测试

至少运行：

```bash
corepack pnpm --filter @thunder-uno/client-web typecheck
corepack pnpm --filter @thunder-uno/client-web test
corepack pnpm --filter @thunder-uno/client-web build
corepack pnpm --filter @thunder-uno/client-web test:e2e
```

如只做第一轮结构调整，可先运行：

```bash
corepack pnpm --filter @thunder-uno/client-web typecheck
corepack pnpm --filter @thunder-uno/client-web test -- appBoot.test.ts
corepack pnpm --filter @thunder-uno/client-web build
```

## 14. 非目标

本轮设计不做：

1. 不修改 `GAME-RULES.md` 中的规则。
2. 不修改 `CARD-CONFIG.md` 中的牌库配置。
3. 不把规则判断复制到客户端。
4. 不重写 WebSocket 协议。
5. 不重做大厅为营销页。
6. 不为了视觉效果牺牲现有 E2E 稳定性。

## 15. 完成标准

完成后应满足：

1. 大厅仍是可调试、可组局的入口。
2. 对战页背景覆盖整个网页。
3. 对战页没有可上下滑动的菜单栏式结构。
4. 手牌和按钮都在对战区内。
5. 对战页仍支持出牌、摸牌、UNO、抓 UNO、质疑、退出房间和返回大厅。
6. 桌面端和移动端都能完成至少一轮真实试玩。
7. 类型检查、前端测试、构建和 E2E 保持通过。
