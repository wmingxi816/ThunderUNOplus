继续开发《雷霆UNOplus》。

当前项目已经完成 Phase 4I：UI 逻辑与算法补完。

已确认：

1. `apps/client-web` 已修复同色丢弃选牌顺序依赖问题。
2. 前端非法出牌提示已经做了更合理的优先级筛选。
3. `packages/uno-core` 的洗牌策略已经从纯均匀随机改成软随机策略。
4. 相关客户端与核心层测试已经补充。
5. 当前还没有进入正式公开试玩部署收口。

现在进入 Phase 4J：首版公开试玩部署准备。

本阶段目标：

- 梳理本地、局域网和公开试玩的访问方式。
- 明确 HTTPS / WSS、环境变量和反向代理的接入边界。
- 把 README、项目开发文档和 phase 文档同步收口。

本阶段不要做：

- 数据库
- 登录系统
- 商城
- 支付
- 排行榜
- 新玩法规则
- 房内重新开局
- 大规模 UI 重构

请先审计这些文件：

1. `README.md`
2. `docs/项目开发文档.md`
3. `docs/PHASE4I-UI-LOGIC-ALGORITHM-COMPLETION.md`
4. `apps/client-web/src/app/config.ts`
5. `apps/client-web/playwright.config.ts`
6. `apps/game-server/src`

然后完成以下任务：

## 1. 公开试玩准备

补充或更新 README 中的公开试玩 / 预发布准备说明，至少包含：

```txt
1. 本地开发端口。
2. 局域网试玩端口。
3. 公开访问需要改成什么地址。
4. `ws://` 和 `wss://` 的区别。
5. 需要哪些环境变量。
6. 浏览器访问地址应该怎么写。
```

## 2. HTTPS / WSS 说明

补充明确说明：

```txt
1. 当前本地开发使用 ws。
2. 公开访问或 HTTPS 页面需要 WSS。
3. 哪些地方要配反向代理或证书。
4. 现阶段只是准备说明，不做正式部署。
```

## 3. 文档输出

新增：

```txt
docs/PHASE4J-PUBLIC-TRIAL-DEPLOYMENT.md
```

同时更新：

```txt
README.md
docs/项目开发文档.md
```

## 4. 测试要求

至少运行：

```bash
corepack pnpm --filter @thunder-uno/client-web typecheck
corepack pnpm --filter @thunder-uno/client-web test
corepack pnpm --filter @thunder-uno/client-web build
corepack pnpm --filter @thunder-uno/client-web test:e2e
corepack pnpm typecheck
corepack pnpm test
```

最后输出 Phase 4J 交付清单。

