# 临时对战界面网格说明

当前为了精确调整对战界面的尺度和位置，临时在对战页面绘制了调试网格。该网格只用于 UI 调参，后续正式版本需要删除。

## 已添加的 DOM

文件：`apps/client-web/src/main.ts`

在 `renderBattlePanel()` 的对战根节点内添加了：

```ts
${renderBattleDebugGrid()}
```

新增函数：

```ts
function renderBattleDebugGrid(): string
```

该函数会生成：

1. 一个覆盖整个对战界面的 `.battle-debug-grid` 容器。
2. 左上角标题 `.battle-debug-grid-title`，内容为 `GRID 40px / 10%`。
3. 顶部横向像素标签 `.battle-debug-label-x`，每 40px 显示一次 `x=...px`。
4. 左侧纵向像素标签 `.battle-debug-label-y`，每 40px 显示一次 `y=...px`。
5. 底部横向百分比标签 `.battle-debug-percent-x`，显示 `0%` 到 `100%`。
6. 右侧纵向百分比标签 `.battle-debug-percent-y`，显示 `0%` 到 `100%`。

## 已添加的 CSS 框线和网格

文件：`apps/client-web/src/styles.css`

### `.battle-debug-grid`

作用：覆盖整个对战界面，但不影响点击。

关键属性：

```css
position: absolute;
inset: 0;
z-index: 80;
pointer-events: none;
```

绘制内容：

1. 黄色外框：`border: 2px solid rgba(248, 214, 78, 0.9)`。
2. 40px 横向小网格线。
3. 40px 纵向小网格线。
4. 10% 横向大网格线。
5. 10% 纵向大网格线。
6. 页面中心竖向黄色中心线。
7. 页面中心横向黄色中心线。
8. 内侧暗色描边：`box-shadow: inset 0 0 0 1px rgba(8, 11, 15, 0.8)`。

### `.battle-debug-grid-title`

作用：左上角说明当前网格规格。

显示内容：

```txt
GRID 40px / 10%
```

### `.battle-debug-label`

作用：像素坐标标签的通用样式。

### `.battle-debug-label-x`

作用：顶部横向像素坐标标签。

示例：

```txt
x=0px
x=40px
x=80px
```

### `.battle-debug-label-y`

作用：左侧纵向像素坐标标签。

示例：

```txt
y=0px
y=40px
y=80px
```

### `.battle-debug-percent`

作用：百分比坐标标签的通用样式。

### `.battle-debug-percent-x`

作用：底部横向百分比标签。

示例：

```txt
0%
10%
20%
```

### `.battle-debug-percent-y`

作用：右侧纵向百分比标签。

示例：

```txt
0%
10%
20%
```

## 后续删除方式

正式完成 UI 调参后，删除以下内容即可移除调试网格：

1. 删除 `apps/client-web/src/main.ts` 中 `renderBattlePanel()` 里的 `${renderBattleDebugGrid()}`。
2. 删除 `apps/client-web/src/main.ts` 中整个 `renderBattleDebugGrid()` 函数。
3. 删除 `apps/client-web/src/styles.css` 中所有 `.battle-debug-*` 相关样式。

